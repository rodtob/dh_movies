<!DOCTYPE html>
<html>
<%-include('partials/head') %> 
  <body>
    <h1><%= title %></h1>
      <section>
      <p>
        <%= pelis.title %> 
      </p>
      <p>
        <%= pelis.rating %>
      </p>
      <p>
        <%= pelis.genero == null ? 'no tiene genero' : pelis.genero.name %>
      </p>
      </section>
      <%-include('partials/footer') %> 
  </body>
</html>
